# Core + tests (xai package and pytest suite)
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
seaborn>=0.12
matplotlib>=3.7
joblib>=1.3
nbformat>=5.9
nbconvert>=7.0
pytest>=7.4
pytest-cov>=4.1

# Notebook only (XAI methods + POS tokenizer + notebook runtime)
shap>=0.44
eli5>=0.13
lime>=0.2
spacy>=3.7
jupyter>=1.0
ipykernel>=6.0
# spaCy English model (not a plain PyPI package - install it separately with):
#   python -m spacy download en_core_web_sm
