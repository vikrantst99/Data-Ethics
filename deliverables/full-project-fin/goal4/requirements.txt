# Core (agent orchestration + Goal-2 model, always needed)
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
joblib>=1.3
langgraph>=0.2

# LLM verdict phrasing (optional - falls back to a template without it)
langchain-core>=0.3
langchain-groq>=0.2
# Set the key before running:  export GROQ_API_KEY=...

# Semantic RAG retrieval (optional - falls back to TF-IDF keyword search)
langchain-huggingface>=0.1
langchain-chroma>=0.1
sentence-transformers>=2.2

# UI options (optional)
ipywidgets>=8.0        # notebook front-end (agent/ui.py)
streamlit>=1.30        # web front-end (streamlit_app.py)
matplotlib>=3.7        # analysis charts in the Streamlit UI (charts.py)

# PDF upload (optional - lets the UI read a proposal from a PDF)
pdfplumber>=0.10       # primary PDF text extractor (agent/pdf.py)
pypdf>=4.0             # fallback PDF text extractor

# Tests
pytest>=7.4

# The Goal-3 weakness pre-gate (agent/certify.py) imports the sibling goal3/xai
# package - no extra install, but its own requirements must be met to run it.
